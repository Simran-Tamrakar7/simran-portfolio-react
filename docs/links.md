# Contact links

Source of truth: `personalInfo` in `src/data/portfolioData.js`.

Used as real URLs (not plain text) in:

- Hero social icons
- CTA “Connect on LinkedIn”
- Contact method cards
- Interactive CV header (`ResumeModal`)
- Footer (GitHub / LinkedIn / Email)
